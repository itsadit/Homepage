import React from "react";
const SearchInput = () => {
    return (
        <div>
                <input
                    className="form-control me-2"
                    type="search"
                    placeholder="Search"
                    aria-label="Search"
                />
        </div>
    );
};

export default SearchInput;